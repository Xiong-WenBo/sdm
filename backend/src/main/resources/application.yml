spring:
  datasource:
    url: jdbc:mysql://localhost:3306/sdm?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
    username: root
    password: X20040706wb   # 修改为你的 MySQL 密码
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.sdm.backend.entity
  configuration:
    map-underscore-to-camel-case: true

jwt:
  secret: mySecretKeyForDormitorySystem1234567890!  # 至少32字符
  expiration: 86400000  # 24小时